## Build-app course mode

When updating **Concepts introduced**, also record **theoretical refresh topics** explained in this episode (even short recaps), at granular level — e.g. "fixed-size chunking + overlap as boundary preservation (EP02)", "pgvector vector(1536) column contract (EP02)".

Later episodes must treat those as **callback only** — do not re-teach the same mental model.

Still record implementation concepts (files, APIs, patterns) as before.

Prefer Demo walkthrough / script facts over generic tool names.
